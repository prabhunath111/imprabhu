name: prabhu_portfolio
description: "Prabhu Nath Tiwary - Associate Lead Engineer Portfolio (Flutter Web)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '3.12.2'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  http: ^1.6.0
  url_launcher: ^6.3.1
  intl:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
